# Pin-free requirements mirroring pyproject.toml.
# Preferred install is `uv sync`; this file is for plain `pip install -r`.

# Core RAG
llama-index-core
llama-index-vector-stores-chroma
llama-index-embeddings-ollama
llama-index-llms-ollama

# Vector store
chromadb

# Data parsing
beautifulsoup4
lxml
python-dateutil
requests

# Journal PDFs → images
pymupdf

# Handwriting OCR (Claude vision via the Anthropic API)
anthropic
Pillow

# Config / secrets
python-dotenv

# UI + CLI niceties
streamlit
tqdm
rich
